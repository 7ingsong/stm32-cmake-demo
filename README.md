# STM32 F1 cmake-based project template + openocd

## Deployment

1. Install STM32CubeIDE 
2. Install OpenOCD 

    ```bash
    brew install openocd
    ```
3. Install VS Code extensions:
    * Tasks
    * Cortex-Debug

## Build & Debug
1. Build (Command+Shift+B)
2. Debug with OpenOCD (F5)
